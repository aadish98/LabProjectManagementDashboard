[package]
name = "lab-workflow-desktop"
version = "0.1.0"
description = "Cross-platform desktop shell for the lab workflow."
authors = ["Cursor Agent"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
